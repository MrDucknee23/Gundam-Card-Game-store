{
  "name": "gundam-card-game-store",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev:admin": "pnpm --filter client-admin dev",
    "dev:customer": "pnpm --filter client-customer dev",
    "dev:server": "pnpm --filter my-backend dev",
    "dev": "pnpm run dev:server & pnpm run dev:admin & pnpm run dev:customer"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@tailwindcss/oxide",
      "bcrypt",
      "esbuild"
    ]
  }
}
